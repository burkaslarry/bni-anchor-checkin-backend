# Copy to .env and fill in values. For Render, set these in Dashboard → Service → Environment.

# Required: use render profile so application-render.properties and DB env vars are used
SPRING_PROFILES_ACTIVE=render

# Render / PostgreSQL (required when using render profile)
DATABASE_JDBC_URL=jdbc:postgresql://YOUR_HOST:5432/YOUR_DB?connectTimeout=5&socketTimeout=10
DATABASE_USERNAME=your_db_user
DATABASE_PASSWORD=your_db_password

# Optional: DeepSeek API for guest/member matching
DEEPSEEK_API_KEY=

# Optional: Render sets PORT automatically for web services
# PORT=10000
